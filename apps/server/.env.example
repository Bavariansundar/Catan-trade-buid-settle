# Copy to .env for local development. See docker-compose.yml for matching values.

DATABASE_URL="postgresql://hexhaven:hexhaven@localhost:5432/hexhaven"
REDIS_URL="redis://localhost:6379"

# openssl rand -hex 32 (or similar) — any long random string in production.
JWT_ACCESS_SECRET="dev-access-secret-change-me"
JWT_REFRESH_SECRET="dev-refresh-secret-change-me"

PORT=3001
